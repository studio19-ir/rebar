name: rebar_calc_pro
description: اپ محاسبه میلگرد + پیام‌های یادآور تبلیغاتی (آفلاین)
publish_to: "none"
version: 1.0.0+1
environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.3.0
  share_plus: ^7.2.1
  flutter_local_notifications: ^17.2.1
  carousel_slider: ^4.2.1
  timezone: ^0.9.2

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/data/messages.json
